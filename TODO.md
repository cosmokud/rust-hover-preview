# TODO

## Features

- **File Format Support:**
  - Design & project files (`.psd`, `.xcf`, `.ai`)
  - Texture & HDR image files (`.dds`, `.exr`, `.hdr`)
- Font previews: sample lines for the scripts there are none for yet (Arabic, Hebrew, Thai, Devanagari), and a way to choose which face of a `.ttc` is previewed.
- Add preview integration for voidtools Everything search.
- Implement automatic updates.
- Fall back to native Windows playback for video files when FFmpeg is missing.
